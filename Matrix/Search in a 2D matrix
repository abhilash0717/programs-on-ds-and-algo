class Solution {
    public boolean searchMatrix(int[][] matrix, int target) {
        for(int i=0;i<matrix.length;i++){
            int[] temp = matrix[i];
             for(int j=0;j<temp.length;j++){
                if(temp[j] == target) return true;
                }   
        }
        return false;
    }
}
